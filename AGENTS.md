# AGENTS.md

Guidance for coding agents (Codex and others) working in this repository lives in
**[CLAUDE.md](CLAUDE.md)** — read it in full before changing anything. It is the
single source of truth for the architecture, the Brazilian-Portuguese-only rule,
the check suites and the correctness invariants.

This file is deliberately a pointer, not a copy: the two used to drift (a stale
copy described a 40-verb subjunctive core and a single-key store long after both
had changed). Do not paste CLAUDE.md's content here; edit CLAUDE.md.
